version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
        - env | grep -e CUSTOM_AWS_ >> .env.production
        - env | grep -e AWS_REGION >> .env.production
        - env | grep -e PYTHON_ >> .env.production
        - env | grep -e NEXTAUTH_ >> .env.production
        - env | grep -e JWT_ >> .env.production
        - env | grep -e GOOGLE_ >> .env.production
        - env | grep -e FIREBASE_ >> .env.production
        - env | grep -e CLOUDINARY_ >> .env.production
        - env | grep -e SPORTRADAR_ >> .env.production
        - env | grep -e NEXT_PUBLIC_ >> .env.production
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
